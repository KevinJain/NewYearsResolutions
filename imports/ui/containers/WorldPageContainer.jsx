import WorldPage from '../pages/WorldPage.jsx'
import { createContainer } from 'meteor/react-meteor-data'

const WorldPageContainer = createContainer(() => ({}), WorldPage)

export default WorldPageContainer
